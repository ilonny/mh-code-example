import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    height: '100%',
    maxWidth: 66,
    alignItems: 'center',
    justifyContent: 'center',
    // padding: 16,
  },
  icon: {
    width: 16,
    height: 16,
    // maxHeight: 16,
    // maxWidth: 16,
    // flex: 1,
  },
});
